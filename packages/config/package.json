{
  "name": "@svcmap/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared config schema and loader for svcmap",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "js-yaml": "^4.1.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/fs-extra": "^11.0.4"
  }
}
