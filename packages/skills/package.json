{
  "name": "@svcmap/skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skill library — atomic, reusable AI operations for svcmap",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest"
  },
  "dependencies": {
    "@svcmap/providers": "workspace:*",
    "zod": "^3.23.0",
    "handlebars": "^4.7.8"
  }
}
