{
  "name": "@svcmap/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Agent orchestrators — compose skills to generate knowledge bases",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@svcmap/providers": "workspace:*",
    "@svcmap/skills": "workspace:*",
    "fs-extra": "^11.2.0",
    "ora": "^8.1.1",
    "chalk": "^5.3.0",
    "p-limit": "^6.1.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4"
  }
}
